class Router {
    constructor() {
        // if (window.Router) {
        //     throw Error('router has been inited');
        // }
        // window.Router = this;
    }
}

export default Router;